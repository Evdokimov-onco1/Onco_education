# Карта проекта Onco_education

Обновлено: 2026-09-07 · по состоянию на коммит `259b0d8`

Каждый пункт помечен: **[прочитано]** — видел в коде, со ссылкой `файл:строка`,
или **[предположение]** — вывел, не проверял.

## Точки входа

- Сайт — единственный `index.html`, весь скрипт в одном блоке с `index.html:171`.
  Сборки нет, зависимостей нет. **[прочитано]**
- `boot()` — `index.html:230`. Тянет `fos-data.json`, при неудаче показывает
  сообщение об ошибке и останавливается (`index.html:232`). **[прочитано]**
- GitHub Actions: `deploy-timeweb.yml` (деплой), `devin-kickoff.yml` (запуск Devin). **[прочитано]**

## Поток данных

```
fos-data.json ──fetch──▶ D (в памяти) ──▶ отрисовка
                            ▲   │
              Firebase RTDB ─┘   └─▶ localStorage (запасной слой)
```

- Структура и номера заданий читаются из `fos-data.json` один раз при загрузке —
  `index.html:231`. **[прочитано]**
- Живое состояние синхронизируется через Firebase RTDB, адрес зашит в
  `index.html:200`. Функции `fbGet`/`fbPut`/`fbDel` — `index.html:204–206`. **[прочитано]**
- `pullRemote()` (`index.html:207`) вызывается при старте, затем каждые 8 секунд
  и при возврате фокуса на вкладку — `index.html:234`. **[прочитано]**
- Два ключа localStorage: `fos-state-v4` — распределение (`index.html:179`),
  `fos-prog-v1` — прогресс по заданиям (`index.html:182`). **[прочитано]**
- `parseNums()` (`index.html:186`) разворачивает записи вида `"2, 6, 52–80"`
  в список номеров. Принимает и тире, и дефис. **[прочитано]**

## Границы

- **Firebase RTDB** — внешний сервис, адрес захардкожен, без авторизации в коде.
  Отвалится — сайт продолжит работать на localStorage: все вызовы обёрнуты
  в `catch` (`index.html:204–206`). **[прочитано]**
- **Google Drive** — папка `1NMOVu7uhu1TAbN3jm3YKBlswrtEaaG-A`, содержание ФОС пишется там.
  Сайт только даёт ссылки из поля `drive`. **[прочитано]**
- **Timeweb** — FTP-деплой при пуше в `main`, и только при изменении `index.html`
  или `fos-data.json` (`deploy-timeweb.yml`, блок `paths`). **[прочитано]**
- **GitHub Pages** — второй адрес того же сайта. **[предположение]**

## Инварианты

- Число элементов в `themes` и ключи ФОС (`onk1..onk4`, `him1`, `hir2`, `lt2`, `mam2`)
  не менять — на них завязана отрисовка матрицы. **[прочитано]**
- `owner` каждой темы обязан быть ключом из блока `teachers`: по нему считается
  нагрузка (`index.html:236`) и прогресс (`index.html:196`). Опечатка в фамилии —
  тема молча исчезает из статистики врача. **[прочитано]**
- `fos-data.json` обязан лежать рядом с `index.html`: путь относительный
  (`index.html:231`). Переложить в подпапку — сайт покажет ошибку. **[прочитано]**
- Деплой не сработает, если правка не задела `index.html` или `fos-data.json`. **[прочитано]**

## Открытые вопросы

- `README.md` утверждает «ни сервера, ни Notion, ни VPN», а `DEVIN.md` (задача 4)
  описывает синхронизацию через присылание файла координатором. И то и другое
  устарело: с коммита `15e59d3` состояние ходит через Firebase.
  **Расхождение документации с кодом — чинить документацию.** **[прочитано]**
- Правила доступа к базе Firebase в репозитории не видны — writable ли она для всех,
  по коду определить нельзя. **[предположение]**
- Включён ли GitHub Pages фактически — из репозитория не проверяется. **[предположение]**
