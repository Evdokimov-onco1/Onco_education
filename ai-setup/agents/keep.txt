# Агенты, которые остаются. По одной строке; совпадение по вхождению в имя файла,
# поэтому короткого имени достаточно: code-reviewer поймает engineering-code-reviewer.md
#
# Остальное prune-agents.sh переносит в ~/.claude/agents-archive/ — не удаляет.
# Передумали — верните файл обратно, ничего не потеряно.
#
# Критерий отбора: агент остаётся, только если он делает то, чего не делают
# ни скиллы с аккаунта, ни команды из этого репозитория. Дублирование хуже
# пустоты: при двух механизмах на одну задачу модель выбирает непредсказуемо.

code-reviewer
privacy-engineer
academic-stat
