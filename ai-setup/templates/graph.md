# Карта проекта

Обновлено: <дата> · по состоянию на коммит `<хеш>`

Каждый пункт помечен: **[прочитано]** — видел в коде, со ссылкой `файл:строка`,
или **[предположение]** — вывел, не проверял. Смешивать нельзя.

## Точки входа

- <что запускается первым> — `файл:строка` **[прочитано]**

## Поток данных

```
<источник> → <обработка> → <результат>
```

- <шаг> — `файл:строка` **[прочитано]**

## Границы

Что снаружи проекта и им не контролируется — внешние сервисы, API, файлы, деплой.

- <граница> **[прочитано]**

## Инварианты

Что сломается, если тронуть.

- <инвариант> — почему важно **[прочитано]**

## Открытые вопросы

Непонятое при построении карты. Не выдавать за факты.

- <вопрос> **[предположение]**
